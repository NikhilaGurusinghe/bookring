info:
  name: random webring site
  type: http

http:
  method: GET
  url: https://nikhilagurusinghe.github.io/bookring/?action=random
  params:
    - name: action
      value: random
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
