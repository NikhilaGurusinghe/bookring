info:
  name: previous webring site
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/?action=previous
  params:
    - name: action
      value: previous
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
