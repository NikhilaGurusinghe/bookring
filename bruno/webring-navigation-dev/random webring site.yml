info:
  name: random webring site
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/?action=random
  params:
    - name: action
      value: random
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
