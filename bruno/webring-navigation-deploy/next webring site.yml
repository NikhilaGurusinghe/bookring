info:
  name: next webring site
  type: http
  seq: 1

http:
  method: GET
  url: https://nikhilagurusinghe.github.io/bookring/?action=next
  params:
    - name: action
      value: next
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
